# SQLLens v4 M3 冒烟执行清单（QA / swat-qa-air）

> 目标包：linux/arm64 Docker 镜像（2026-09-04 早晨交付）。
> 执行分工：M3（arm64）最终冒烟由卢西恩执行；QA 本机对 amd64 镜像做逻辑等值冒烟。
> 判定：每项记录 通过/失败，附命令与原始输出；容器能启动不等于通过。

## 0. 交付物核对

- [ ] 镜像架构为 linux/arm64：`docker image inspect <img> --format '{{.Architecture}}'` = `arm64`
- [ ] 启动说明（README/命令）可照抄执行，无手工补丁步骤
- [ ] 本清单随包交付

## 1. 包启动

- [ ] `docker run` 一键启动成功，Web 可访问：`curl -fsS http://127.0.0.1:18080/` → HTTP 200
- [ ] `/healthz` 返回 200
- [ ] 宿主仅发布回环端口：`docker port <c>` 只见 `127.0.0.1:18080`；`0.0.0.0`/`::` 不得出现
- [ ] 重启容器后 Web 仍可访问（数据卷保留性符合说明）

## 2. 两屏主流程（输入 → 诊断 → 报告）

- [ ] 输入屏：TiDB 连接配置可填、「测试连接」可用
- [ ] 「生成只读账号脚本」一键复制，脚本为最小权限（SELECT/PROCESS）
- [ ] 双输入 tab：在线慢 SQL 选择 / 粘贴 SQL 均可提交
- [ ] Plan Replayer zip 上传入口 + 一键 DUMP 脚本复制可用
- [ ] 配置缓存：重新打开页面自动带出；「清除配置」可清空
- [ ] 报告屏：诊断完成后展示报告，三份样例可切换，证据点击溯源弹层可用

## 3. 规则诊断与数字一致性

- [ ] 三类规则（索引访问/统计偏差/热点重复）各出一份报告
- [ ] 样例数字与 fixture 一致：842 次调用/平均扫描 126 万行/3150:1；estRows 120 vs 18 万行
- [ ] P 级口径：默认 P2；仅 p95≥5s 且执行≥20 次升 P1；无 P0
- [ ] 报告六段完整：结论 / 证据 / 问题分析 / 变更建议（四要素：操作·风险·成本预估·收益预估）/ 验证 / 回滚
- [ ] 四要素附确定性计算公式（非拍脑袋数字）

## 4. AI 配置（可选，不配置默认规则模式）

- [ ] 不配置 AI 时走规则模式，报告正常出
- [ ] 「连接测试」：成功/失败路径均有反馈
- [ ] 「识别模型」：拉取模型列表并可选
- [ ] 五类报错演示：网络不可达 / 401 / 403 / 模型不存在 / 协议不兼容，逐项触发页面报错
- [ ] AI 调用失败自动降级规则输出并在报告页顶部提示

## 5. 安全边界

- [ ] 默认只读：DML/DDL/`EXPLAIN ANALYZE`/自动执行建议被拒绝（提交后返回错误、不落到 TiDB）
- [ ] 只读账号审计：TiDB 侧实际发出的语句全部为 SELECT/统计类，无写入
- [ ] 配置加密缓存：volume 中无明文 token/key（`docker exec` 检查缓存文件为密文）
- [ ] 诊断数据不落盘：SQL 文本、证据、报告仅会话内存，页面关闭/容器重启后不残留
- [ ] 日志无明文凭据：`docker logs` 扫描无密码/key 原文
- [ ] 端口仅 localhost：局域网地址访问失败

## 6. 结果汇总

| 项 | 结果 | 证据 |
|---|---|---|
| 0 交付物核对 | | |
| 1 包启动 | | |
| 2 两屏主流程 | | |
| 3 规则诊断 | | |
| 4 AI 配置 | | |
| 5 安全边界 | | |

最终结论：全部通过才可交付；任一失败附失败原因与复现步骤，退回 RD 修复。
